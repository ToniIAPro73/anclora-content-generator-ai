# ANCLORA-FEAT-003

Feature de migracion de autenticacion desde `Supabase Auth` hacia `Better Auth + Neon`.

Estado actual:

- Dependencia `better-auth` instalada.
- Skeleton server/client preparado.
- Handler `/api/auth/[...all]` creado en modo coexistente.
- Migracion de tablas y login real pendiente de la siguiente iteracion.
